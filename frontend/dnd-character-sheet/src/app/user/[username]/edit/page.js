// Layout Page for Admin
'use client'

import { useParams } from 'next/navigation'

import EmailModifier from '@/components/EmailModifier';

export default function UserPage() {
    
    return (
        <EmailModifier />
    );
}

